{% extends "base.twig" %}

{% block title %}{{ parent() }} — Upload file{% endblock %}
{% block header %}Upload file{% endblock %}

{% block body %}
    {% if app.session.hasFlash('error') %}
    <p>{{ app.session.getFlash('error') }}</p>
    {% endif %}
    <form action="/upload" method="post" enctype="multipart/form-data">
        <p><input type="file" name="cdn_file" /></p>
        <p><button>Submit</button></p>
    </form>
{% endblock %}
